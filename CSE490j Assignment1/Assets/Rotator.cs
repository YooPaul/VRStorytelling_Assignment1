using UnityEngine;
using System.Collections;

public class Rotator : MonoBehaviour
{

    void Update()
    {
       // transform.Rotate(new Vector3(0,0,30) * Time.deltaTime);
       // transform.rotation = Quaternion.AngleAxis(30, Vector3.right * Time.deltaTime) ;
    }
}